generator client {
  provider = "prisma-client-js"
}

generator erd {
  provider = "prisma-erd-generator"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Example table
model User {
  id    String @id @default(cuid())
  name  String
  email String @unique
}
